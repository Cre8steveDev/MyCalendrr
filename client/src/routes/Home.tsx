/**
 * Home Component - This renders the landing
 * page that users are met with when they
 * visit the index page.
 * @returns
 */
const Home = () => {
  return <div>Home</div>;
};

export default Home;
